# 体調管理アプリ 仕様書

> このドキュメントはアプリの現状仕様をまとめた常設リファレンスです。
> GitHub 上でいつでも閲覧でき、コードと一緒にバージョン管理されます。
> 最終更新時点のアプリバージョン: **v14.87**

---

## 1. 概要

「体調管理」は、体調・睡眠・飲酒・食事・運動・タスク・目標などを一元的に記録・
振り返るための**パーソナル健康管理 PWA**（個人用）。シフト勤務（1直/2直/休み）を
前提にした予定管理と、減酒・生活改善の行動支援機能を備える。

- **利用形態**: スマホ／PCのブラウザ、およびホーム画面に追加した PWA として動作
- **対象ユーザー**: 本人のみ（個人利用）
- **オフライン対応**: Service Worker によりオフラインでも起動・記録が可能
- **端末通知**: 切り替え提案（ナッジ）をローカル通知でも受け取れる（許可制・ベストエフォート）。
  **通知する項目・時刻・静かな時間帯は設定画面の「🔔 通知」でまとめて設定**

---

## 2. 技術構成

| 項目 | 内容 |
|---|---|
| 実装形態 | **単一ファイル** `health.html`（約 840KB）にHTML/CSS/JSをすべて内包 |
| フレームワーク | 素のバニラJS（ビルド不要）。DOM生成ヘルパー `h()/div()/btn()/span()/txt()` |
| 描画 | 状態変化のたびに `render()` で `#main-content` を再生成する全描画方式。同一画面ではスクロール位置を保持。描画後に`tintSurfaces()`でモード色のトーン置換＋`outlineDarkText()`で**黒など極端に暗い文字色に白い縁取り**（色は変えない。暗い下地のときだけ／淡いグレーの文字や明るいブロック上の濃い文字は対象外）。
`var(--mode)`など変数で指定された文字色も`getComputedStyle`で解決して対象にし、**モードの色を黒にしても文字が読める**。
ヘッダーは`render()`で作り直さないため、毎回`#app-header`にも同じ縁取りをかけ直す。
自分で付けた縁取りには印（`data-outlined`）を残し、**モードの色が明るくなったら外す**（外さないとヘッダーに縁取りが残り続ける。他所で付いた影には触らない） |
| データ保存 | **localStorage のみ**（サーバーDBなし）。キー接頭辞は `health_v2` |
| AI連携 | 外部プロキシ `api.php`（ConoHa上）経由で Google Gemini を呼び出し |
| 配信 | **GitHub Pages**（`https://yuradream3838.github.io`、独自ドメインなし） |
| バージョン管理 | `const APP_VERSION` 1箇所で全体に反映。表示・キャッシュ更新に使用 |

### リポジトリ構成
```
health.html      … アプリ本体（全機能）
index.html       … ルート(/)アクセスを health.html へ転送
manifest.json    … PWAマニフェスト（standalone・ショートカット）
sw.js            … Service Worker（HTMLは常に最新取得、オフライン時のみキャッシュ）
icon.svg         … アイコン
SPEC.md          … 本仕様書
test/            … 回帰テスト（構文チェック＋スモークテスト）
package.json     … テスト実行スクリプト
※ api.php はサーバー(ConoHa)側にのみ置き、リポジトリには含めない
```

---

## 3. 画面構成（タブとサブタブ）

下部ナビの5タブ＋ヘッダーの設定ボタン。既定の起動画面は「今日のウィジェット」。

| タブ | サブタブ | 内容 |
|---|---|---|
| **📅 予定** | 日 / 週 / 月 / 🔍検索 | 予定と実績のタイムライン。横断検索で各設定へジャンプ |
| **📔 日記** | 生活記録 / 睡眠 / トレーニング / 体調記録 / アルコール | 日々の記録。各分野別 |
| **✅ タスク** | ルーティン / 定期ルーティン / 目標 / TODO / 買物 | やること・習慣・目標の管理 |
| **🏆 達成** | 達成 / 未来投資 / つながり / 動機づけ面談 / if-then | 継続・行動支援・動機づけ |
| **📊 分析** | 日次・週次 / 睡眠 / トレーニング / 総合分析 / AI分析 | 振り返り・過去の履歴・AI分析 |
| **⚙ 設定** | （単一画面） | 機能目次・**最近の保存**・モード・**🔔 通知**・**🔊 Alexa連携**・表示・色・シフト・パターン・就寝リズム・ルーティン編集・データ管理 |

**設定画面の各項目はたためる**（`collapsibleSetting`／`state.setOpen`・`_setopen`に保存）。見出しをタップで開閉し、
右端に▼/▶を表示。**既定はすべて閉じた状態**で、右上の「⊕ すべて開く／⊖ すべて閉じる」で一括切替できる。
就寝リズムへのジャンプ（`goToSleepSettings`）や通知へのジャンプ（`goToNotifySettings`）、モード設定からの遷移は、その項目を自動で開く。

**🕘 最近の保存（`renderSaveHistoryPanel`）**：**保存したものすべて**（予定・実績・体調記録・飲酒・射精・ルーティンの実施に加え、
目標・TODO・定期ルーティン・ルーティン設定・シフト・パターン・就寝リズム・モードの設定/目標/タグ・体組成・未来投資・
if-then・つながり・MI各種・ウィジェット表示など）を**新しい順に最大10件**表示する（時刻・種類・対象日）。
対象は`SAVE_TARGETS`の表（保存関数・復元する`state`フィールド・ラベル）で定義し、起動時に各保存関数を包んで`recordSave()`を呼ぶ。
`UNDO_FIELDS`（＝戻すで復元する範囲）と`persistAll()`もこの表から自動生成されるので、**記録できるものは必ず戻せる**。
`setLog`／`setDaily`／`setTodo`は個別にフックしてラベルを細かく出す（🩺体調記録／📌実績／🍺飲酒／💧射精／✅ルーティン名 など）。
保存関数は変更の**あと**に呼ばれるため、`_prevSnap`（前回記録した時点の状態）をこの変更の「前」として使う。
同じ対象への8秒以内の連続保存は1件にまとめる（戻すとその一連の変更の前に戻る）。
**戻せるのはいちばん新しい1件だけ**（`restoreSave`。スナップショットが全体のため選択的な取り消しはできない）で、
押すたびに1つずつさかのぼれる。戻した操作自体は履歴に積まない。
スナップショットは容量が大きいため**localStorageには保存せず、アプリを開いている間だけ保持**する（`state.saveHist`）。
起動時の読み込み・移行処理は`_histReady`で除外し、戻す操作自体は`_histBusy`で記録しない。
ナッジの当日状態・自動切替の内部状態・設定の開閉は履歴の対象外（内部状態のため）。
モードの**切り替え**も履歴には載せないが、**手で直したモードの時間**（🕒 モードの時間）は載り、戻せる（4-8参照）。

**📊 分析タブは「日次・週次／睡眠／トレーニング／総合分析／AI分析」の5つ**。内容が重複していた**週次分析は日次ログへ統合**し、
統合後のタブは上から **🕒 モード別の時間（1日/1週間/1ヶ月・過去へ移動可）→ 🔎 モード時間と体調の関係 → 🩹 ケアの履歴
→ 📅 週の推移（体調スコア／睡眠時間／飲酒／TODO達成率の日別グラフ＋週のまとめ・‹ ›で過去8週）
→ 月間体調ヒートマップ → 日別の記録カード（直近30日）** の順に並ぶ。
旧サブタブ名（日次ログ・週次分析）は開いたときに新しいタブへ自動で読み替える。

### 今日のウィジェット（既定ランディング）
今日の予定・ルーティン・目標などをまとめて素早く確認/チェックできるホーム画面。
表示項目は⚙️から選択・並び替え可能（全17項目、一部は既定オフ）。
`#widget` / `#today` ハッシュで直接起動でき、ホーム画面ショートカットからも開ける。

---

## 4. 機能詳細

### 4-1. 予定
- 直接予定（単発）・繰り返し予定（毎日/毎週/毎月）
- **👀 参照するだけ**（予定の追加・編集モーダルのトグル `refOnly`）：行動ではなく「見るための情報」として置く予定。
  通常の予定は**完了ベース**でグレーアウトする（実績に反映／チェック／連動ルーティン完了）が、
  参照するだけの予定は完了の概念がないので**終了時刻を過ぎたら自動でグレーアウト**する（`planRefPassed`）。
  タイトル先頭に👀が付き、完了の✓は出ない。情報画面では「✓ 実際に行った（実績に反映）」を出さず説明文に差し替える。
  終日の場合はその日を過ぎるまでグレーにならない。過去の日は常にグレー、未来の日は常に通常表示。
- タイムライン表示で「予定」と「実際の行動（実績）」を対比。実績の表示位置は全種類で統一：
  **タイマー式（開始〜終了あり）は開始が上端・終了が下端の帯**、**チェック式（点）はチェック/完了時刻を下端にして上へ一定量伸ばす**。
  レーン割当（重なり回避）と描画は同じ縦位置(px)を用いる（`blockGeom`）ため、実績どうしで表示がずれたり重なったりしない。
  - **🍺 飲酒も、飲み終わりを記録してあれば「飲み始め〜飲み終わり」の幅の帯**で出す（`getActualActivities`）。
    飲み終わり（`endHour`/`endMin`。アルコールタブの「🍵 飲み終わり」・飲みモードの終了で記録される）が
    **無い1杯は今までどおり点**。0時をまたぐ飲み終わりはその日は24時まで、残りを翌日の0時から出す。
    「翌日（深夜）」の飲酒（`startNextDay`）は実際に飲んだ翌日側に出すのも同じ。5分など短すぎる幅は点あつかい。
- ルーティン群（🌅モーニング等）は実績で1ブロックにまとめて表示。ただし**項目どうしの空きが30分以上
  なら別ブロック**として分割表示。
  **まとめても内容（セッションのメモ）は実績に表示される**：そのブロックに含まれる項目のメモを
  「まとめ名／項目名：メモ」の形でタイトルに続けて出す（同じ項目に複数のメモがあれば「項目名：メモ1・メモ2」とまとめ、
  重複は除く）。メモはブロックごとに集計されるので、分割された別ブロックには別のメモが付く。各項目は開始〜終了を1つの区間として扱い、**1件の計測時間が30分以上でも
  その内部では分割しない**（区切りはあくまで別項目との空きで判定）
- **予定列と実績列の幅は「いま」を境に変わる**：**現時点より前は実績列を広く（35:65）／後は予定列を広く（65:35）**。
  過ぎた時間は実績を、これからの時間は予定を見たいため。過去の日は全体が実績寄り、未来の日は全体が予定寄りになる。
  帯は**その中央がいまより前か後か**で幅が決まり、境目には縦線を引くので「いま」の位置で段差になって理由が分かる。
  列見出しの幅と、空き時間タップの左右判定（予定を追加／実績を追加）も同じ境界に合わせる。
  **実績列のモード色帯も同じ境界に追従**し、いまをまたぐモードは境目で切って左右幅を変える（段差が実績ブロックと揃う。
  角丸は外側だけ・アイコンチップは高いほうの段に1つだけ）。
- **✓ やった（実績には出さない・グレーにするだけ）**（`planDone`／`isPlanMarkedDone`／`setPlanMarkedDone`）：
  実績へ反映すると実績列にも同じ帯が並ぶが、それが邪魔なときのために**実績を作らずに完了の印だけ**付けられる。
  予定の帯をタップ→情報画面の「✓ 実際に行った（実績に反映）」の下にあるボタンで付け外しする（パターン予定でも同じ）。
  印は**日ごと**に持ち（通常の予定＝`e`+uid／パターン＝所有日に`p`+patternUid）、`planBlockDone`がいちばん先に見る。
  グレーアウトと✓の見た目は実績に反映した場合と同じ。「最近の保存」には**✓ 予定の完了**として載り、戻せる。
- 終日予定、事前予告（○日後）表示
- 週・月カレンダー
- 横断検索：予定・定期ルーティン・ルーティン・パターン・目標・TODO を名前で検索し設定画面へ移動
- **パターン予定の🔗連動ルーティン**（`linkRoutine`／`patternLinkDone`）：連動先（まとめ・単体の項目・起床・就寝・シフト）が
  完了すると、そのパターン予定の帯がグレーアウトする。**未設定のときは「名前が同じ」まとめ／項目に自動で連動する**
  （`autoLinkRoutine`。絵文字・記号・空白を無視した完全一致。まとめを先に見て、空のまとめは飛ばす）。
  🍳朝食のパターン予定は🍳朝食のまとめに自動でつながるので、設定しなくても全部やればグレーになる。
  設定画面のプルダウンには「自動（◯◯ と連動）」と出る。自動もやめたいときは「連動しない」を選ぶ（`{t:"off"}`）。
- **「この日は非表示」と「除外」は連動する**（`syncPatternHideToRoutines`／`syncRoutineExcludeToPatterns`）：
  どちらも「この日はやらない」という同じ意味なので、片方だけ残らないよう両方向でそろえる。
  **パターン予定を非表示にすると連動ルーティンもその日は除外**になり、**戻すと除外も解除**される。
  逆に**ルーティンを除外していくと、連動するパターン予定が非表示**になる（まとめ連動は**全項目が除外されたとき**。
  1つでも戻せば表示に戻る）。対象日は`patternRoutineDay()`で`planBlockDone`と同じ決め方をする（翌日にずらす設定も追従）。
  連動が「連動しない」や起床・就寝・シフトのものはルーティンを触らない（非表示自体は効く）。
- **🙈「非表示にした予定」の箱**（予定タブ「日」の下）は、その日の画面から消したものを`↩戻す`で戻すためのもの。
  **その日に実際に出るはずだったものだけ**を並べる：当日のぶんに加え、前日以前のパターン予定は
  **0時をまたいでその日に掛かるものだけ**（`patternCoversDay`＝予定の描画とまったく同じ計算。
  `patternTimesFor(p,ds,true)`で非表示を無視した本来の時間を見る）。
  以前は前日・前々日に非表示にしたものを掛かり方に関係なく「（前日分）」として出していたため、
  その日の画面には元々出ない予定の「戻す」だけが並んでいた。
- パターン予定（設定）も**編集は自動的に「今日以降のみ」反映**：変更前の定義を `pastVer` として
  昨日まで自動凍結し、過去の表示（時刻・名前・色）は変わらない。`effectivePattern(p,ds)` が日付ごとに
  有効な定義を解決し、`patternTimesFor` 等が使用（優先順位：その日だけ変更 patternOv → pastVer → 現行）

### 4-2. 日記
- **生活記録**: 体調スコア、飲水量、食事などの記録
- **睡眠**: 就寝・起床、睡眠時間、中途覚醒、昼寝。就寝リズム連動
  - **🌙 中途覚醒**は「その夜の朝の日」に持つ（`awakenings`＝`{hour,min,endHour,endMin,mins}`。0〜12時はその日、12時以降は翌日）。実睡眠から差し引かれる。
  - **😴睡眠 → 🩹ケア → 😴睡眠 と戻ると、そのケアの時間を中途覚醒として自動記録する**（`recordSleepAwakening`）。
    夜中に目が覚めて落ち込み・さみしいモードを使った時間は眠れていない時間なので、実睡眠から引く。
    条件は**睡眠から入ったケアで、睡眠へ戻ったとき**（ケアのあと☀️朝へ出たら起床なので記録しない）。
    1分未満と、同じ時刻の重複は記録しない。記録には`fromMode`／`modeLabel`が付き、一覧にどのケアだったかを小さく表示する。
- **トレーニング**:
  - 体組成記録（体重・体脂肪・BMI・BMR・内臓脂肪・筋肉量・部位別・血圧・脈拍 等14項目）
  - **写真からの自動入力**（InBody/血圧計の画面を撮影→AIが数値抽出。※画像は保存せず数値のみ）
  - ワークアウト記録（種目・負荷・回数・セット）
  - 有酸素記録（ランニングマシン等）：**タイマー式**。開始→途中で「速度変更」すると区間ごとに
    速度×時間を記録し、**距離を自動計算**（速度・距離は小数点2桁）。手入力での追加も可能
  - ルーティンの「トレーニング」項目と連動
  - **過去の履歴は分析→🏋️トレーニング**（`renderTrainHistory`。種目べつの記録・自己ベスト・日ごとの記録。4-7参照）
  - **🏋️トレーニングモードの画面にも、このタブの記録機能をまるごと表示する**（`renderTrainingPanel(vd,...)`を
    そのまま埋め込むので、💪トレーニング（計測・セッションのメモ）／🏋️種目・負荷・回数／🏃有酸素／📊体組成（📷撮影・🖼画像から）が
    同じ操作・同じデータで使える）。体組成の入力モーダルはモード画面の上に重ねて開く。
    モード画面は基準日（`activeDay`）固定なので、他の日を見るための日記タブへの導線も残す。
- **💧 射精**: 時刻・タグ（自慰／セックス）・翌日フラグ・表示色。**記録の追加と編集・削除**ができる（`openEjacModal`／`renderAddEjacModal`。
  一覧の時刻をタップすると編集モーダルが開き、モーダル内から削除も可能）。入口は**生活記録**・**ウィジェットのクイック記録**・**🔥エロモード**の3か所で、すべて同じデータ。
  モード画面からの新規記録は基準日(`activeDay`)へ書くため、**0時を過ぎていると「翌日」を自動でON**にする（飲み物の追加と同じ扱い）。
- **体調記録**: 症状・服薬など（症状の選択肢＝頭痛／肩こり／腰痛／関節痛／筋肉痛／吐き気／胃もたれ／めまい／目の疲れ／鼻づまり／眠気／倦怠感／集中力低下／不安感／さみしい／気分良好／やる気充実／頭が冴えている／よく眠れた）
- **アルコール**: 飲酒記録、純アルコール量ゲージ（日/週）、減酒サポート各種（下記4-5）
  - **📝 お酒の記録（テイスティングノート・`drink.tasting`）**：飲み物の追加/編集画面の**いちばん下**に、
    **名前・生産者・生産地・外観・香り・味わい・余韻**（`TASTE_FIELDS`）と**⭐総評**を書ける。
    量の管理とは別に「何を飲んだか・どうだったか」を残すためのもので、**たためる**（記録があれば開いた状態で出る）。
    - **星は0.5刻み＝10段階**（`starPicker`。星の**左半分で◯.5／右半分で◯.0**、同じ値をもう一度押すと解除。
      `onPick`を渡さなければ表示だけの読み取り用になる）。保存時は`roundHalf()`で0.5〜5.0にそろえる。
      文字で書くときは星の数で半分を表せないので**「★4.5」**と数で添える（`starsLabel`）。
    - **🫧 お酒との近さ**（`near`／全然違う〜そっくり・こちらも0.5刻み）は**アルコール度数が0%のときだけ**の項目。
      度数の入力中に描き直すと入力欄からフォーカスが外れるので、表示の出し入れだけを切り替える（`#drink-near-row`）。
      **度数が入っていれば保存時に落とす**（`cleanTasting(t,pct)`）。
    - 空欄だけなら`tasting`自体を持たせない。星は**同じ星をもう一度押すと解除**。
    - アルコールタブのその日の一覧には「📝 名前　★4.5」の1行が出て、**タップで全文を開ける**（`tastingDetailBox`）。
  - **🍶 お酒の記録の一覧**（アルコールタブの下部・`renderTastingLogCard`）：**日をまたいで全部見返せる**。
    - 記録のある1杯だけを集める（`allTastingNotes`）。**新しい順／総評が高い順／近さが高い順**で並べ替え（`sortTastingNotes`）。
    - **検索**は名前・生産者・生産地・外観・香り・味わい・余韻＋種類が対象（`tastingSearchText`）。
      打つたびに画面全体を作り直すと入力欄からフォーカスが外れるので、**一覧の中身だけ**を描き直す（`tastingLogBody`）。
    - 行タップで中身（その日の一覧と同じ`tastingDetailBox`）、右の**📅 日付でその日のアルコールタブへ移動**。
    - 上に**総評の平均**。**たためる**。多いときは20件まで出して「のこり◯件を表示」。

### 4-3. タスク
- **ルーティン（デイリー）**: 毎日の固定項目。チェック式／タイマー式（単発・複数セッション）
  - **設定画面から項目を編集可能**（名前変更・種類変更・追加・削除・並び替え・グループ割当）
  - **まとめ（`DEFAULT_ROUTINES`・実際列で1ブロックにまとめる単位）**：🌅モーニングルーティン／🍳朝食／🍱昼食／🍽夕食／
    **🚿シャワー**／🌙ナイトルーティン／📥インプット／📤アウトプット／🏠帰宅ルーティン。
    既存ユーザーに不足しているまとめは`migrateRoutines()`が🌙ナイトルーティンの手前へ補完する
    （**他のまとめに未割り当ての項目だけ**を入れるので、すでに自分で割り当てた項目は奪わない）。
- **定期ルーティン**: 曜日/間隔/シフトで繰り返す予定。時刻指定・タイマー対応・この日だけの変更。
  その日の分は**既定モード（いつもの生活モード＝`getDefaultMode()`）の画面**に「🔁 定期ルーティン 残り◯件＋繰越◯件」としてまとめて並び、
  **完了したものは非表示**、**過去に該当したのに未実施のもの（繰り越し）は「🔴 繰越◯日前・未実施」として先頭に表示**（`customTodoOccurrence`。未来日では出さない）。
  - **繰り越しを実施したとき**（`completeCustomTodoCarry`）：実施は**やった日**に記録し、直近の完了回までさかのぼって
    未実施の回を除外する（`dismissPastUnchecked`）。除外した日は**完了の記録に`skipped`として残す**ので、
    **完了を取り消せば、まとめて除外した回も元に戻る**（`restorePastSkipped`。`autoSkip`が付いた回だけ戻すので、自分で「除外」した回は触らない）。
  - **時間計測式でも繰り越しを実施できる**：定期ルーティンタブ・ウィジェット・モード画面のどれでも▶開始／⏹終了が出る。
    計測中は「🔴 ◯日前から未実施」ではなく**「⏱ ◯日前(M/D)の分を実施中」**と出す。
  - **完了の取り消しはチェック式・時間計測式とも同じ**（`uncheckCustomTodo`。✓をタップ。時間計測式は開始・終了時刻も消える）。
  - **遅れて実施したあとの「次回」**は項目ごとに選べる（`fromDone`・既定オフ）：
    **オフ**＝起点から機械的に数える（「毎週◯曜」のような予定向け。遅れても次回はずれない）。
    **オン**＝**実施した日を新しい起点**にする（掃除・散髪など「前回から◯日空けたい」もの向け）。
    実装は既存の`reschedules`に`auto`セグメントを積む方式（`reanchorCustomTodo`。直近12件だけ保持）で、
    取り消すと`unanchorCustomTodo`でひとつ前の起点に戻る。日付そのものに意味がある
    **曜日指定（毎週◯曜）・毎月◯日・直ごとの設定では選べない**（`ctFromDoneOk`。編集画面にも出さない）。
  チェック／▶開始・⏹停止の時間計測ができる（時刻ありは時間順・なしは末尾。項目タップでこの日だけの変更）。
  **「除外」した回は予定にも実績にも出さない**（`rec.excluded`。計測済み・計測中でも同じ。ルーティンの除外と同じ扱い）。
  除外は**日ごと**なので、深夜に記録した回（所有日が前日のもの）はその**前日側で除外**する。
  項目ごとの割り当ては行わず、既定モードを変えれば表示先もそこへ移る。
  **予定タブの一覧（📌欄）からは表示を廃止**し、生活モードへ移した（タイムラインの予定ブロック・実績には引き続き表示）。
  **編集は自動的に「今日以降のみ」反映**：定義を変更すると、変更前の定義が「昨日まで」の
  過去バージョン（`pastVer`）として自動凍結され、過去の予定・実績は一切変わらない（名前・時刻・
  周期・色すべて）。基準日は今日固定・全項目が未来のみ・手動の切替UIは非表示。
- **目標**: 年間〜日次の目標ツリー（親子・進捗集計）
  - **達成判定は`goalAchieved(n,vd)`の1本**（以前は`n.done`／全下位done／その両方の3通りが混在し、親を手でチェックしても
    期間ビューでは未達成のまま、といったズレが出ていた）。進捗は`goalGauge(n,vd)`＝**直下の子**で数える。
  - **🎯 達成ルール**（下位目標を持つ目標だけ・`doneRule`）は**2つだけ**：
    - **下位が全部**（既定・`{mode:"all"}`相当＝未設定 / `null`）：**最後の下位目標を達成すると上位目標も達成**になる
    - **自分で判定**（`{mode:"manual"}`）：下位が全部そろっても自動では達成にしない（成果物型。「企画書を仕上げる」など）
    どちらでも**手でチェックすれば達成**にできる（`n.done`が優先）。
  - **ルールは下位目標を作るときに選ぶ**（目標は**上から下へ**作る前提。下位目標ができた時点で
    「この上位目標は下位目標の達成で完成するか」が決まる）。「＋下位目標」の画面に
    **「『◯◯』の達成判定」**として2択を出す（既定は「下位が全部」。2件目以降は**いまのルールが初期値**）。
    あとから**目標の編集画面**でも同じ2択で変更できる（選択UIは`goalRuleChooser()`の1本）。
  - **自動で達成になるのは下→上だけ**。親を手で達成にしても**子は未チェックのまま**にする
    （やっていない日を「やった」ことにすると記録・連続日数・分析が壊れるため）。
  - **表示でも日次と週次を区別**：子から決まる目標（＝積み上げ型）は**チェックボックスを出さずゲージ**（`2/3`）にし、
    達成すると✓が付く。日次目標・単独の目標・「自分で判定」の目標は今までどおりチェックボックス。
  - 既存の目標は`doneRule`未設定＝「下位が全部」なので、**見え方も判定も変わらない**。
    廃止した旧「子のうち◯個」（`{mode:"count",need:N}`）は読み込み・バックアップ復元時に
    `migrateGoalRules()`が**「自分で判定」へ寄せる**。その時点で条件を満たしていたものは`done:true`として残すので、
    **達成済みの目標が勝手に未達成へ戻ることはない**。
  - **📅 予定に追加した目標は、その予定を表示する**（`openGoalAsEvent`→予定に`goalId`が付く／`goalEvents(goalId)`で引く）。
    - 目標の詳細：まだ無ければ「📅 この目標を予定に追加」、**作ってあればボタンを出さず**
      「📅 予定に入れています（◯件）」として**日付・時刻・タイトル・色**を並べ、タップでその日の予定タブへ移動する。
    - 期間別の一覧：📅ボタンの代わりに**その予定の日付**（繰り返しは「繰り返し」・2件目以降は`+n`）を出し、タップで同じく移動。
    - 予定を消せば自動で「予定に追加」へ戻る（保持ではなく**その都度引く**）。既存予定を編集して保存しても`goalId`は引き継ぐ。
    - 単発（`schedule`）と繰り返し（`repeatEvents`）の両方に対応。
  - **目標の期間と、その予定の日付は連動する**（どちらかを動かすともう片方も動く。`_goalDateSyncing`で呼び合いを1回に制限）。
    - **予定の日付を変えた**→`syncGoalFromEvent`が目標の期間を合わせる。
      日次＝**その日**に、週間〜年間＝**その日を含む期間**に（`getGoalKey(type,ds)`）。同じ期間の中で動かしたときは何も変わらない。
      複数日の一日目標（`dailyEnd`）は**長さを保ったまま**ずれる。変わったときは新しい期間をトーストで知らせる。
    - **目標の期間を変えた**→`syncEventsFromGoal`が、その目標の予定を**期間の開始日どうしの差の日数だけ**ずらす
      （週間を1週後ろへ＝予定も7日後ろへ。曜日・時刻は保たれる）。単発・繰り返しの両方に対応し、
      🫂つながる予定の参照（`moveConnectPlanRef`）も一緒に動く。
    - 期間を持たない目標・予定が無い目標・目標と結びついていない予定では何も起きない。
- **TODO**: **「✅ やること」と「✨ やりたいこと」の2つに分ける**（同じ`state.tasks.todo`に`kind`を付けて分けるだけなので、
  期日・重要/緊急・メモ・「📅 予定に追加」など中の機能はどちらでも同じ／`TODO_KINDS`・`todoKindOf()`）。
  - `kind`が無い既存のものは**「やること」扱い**なので、分け方を変えない限り見え方は変わらない
    （v8.3の旧`taskList`＋`wishList`統合で失われていた区別を、この形で戻したもの）。
  - **どちらもたためる**：見出しタップで開閉し、状態は`_todosec`に保存する（`getTodoSecOpen`）。
    **閉じていても未完了の件数（＋完了数）は見出しに出る**。実績などから飛んできて強調するTODOがある側は自動で開く。
  - 追加欄（テキスト・⭐重要・⚡緊急）は**それぞれ別に持つ**ので、片方に書きかけでももう片方の入力は消えない。
  - 各項目の「→ やりたいことへ／→ やることへ」で**入れ替えられる**。「完了済みをまとめて削除」は**その側だけ**。
  - **ウィジェットの✅カードは「やること」だけ**（やりたいことは急がないので日々の画面に出さない）。
    予定タブの期日表示は期日を入れたものが対象なので、やりたいことでも期日を入れれば出る。
- **買物リスト**

### 4-4. 達成
- **達成**: 全ルーティン連続達成日数、ご褒美（連続日数/目標達成でアンロック）
- **未来投資**: 我慢・前向き行動を回数・金額で「貯金」化。踏みとどまりボタン、貯金残高
- **つながり**: 人とのつながり習慣の記録・目標。履歴は種類・内容に加え**日付・時刻も修正可**（別の日への移動に対応）
  - **🗓 つながる予定**は予定タブの予定を`{ds,uid}`で参照する（`resolveConnectPlan`）。
    **もとの予定を編集しても結びつきは切れない**：単発の予定を編集したときは**同じuidのまま**保存し（以前は保存のたびに
    新しいuidになり、参照が迷子になっていた）、日付を動かしたときは`moveConnectPlanRef`が参照の日付も一緒に動かす。
    予定を削除したときは`dropConnectPlanRef`が参照を片付ける。
    繰り返しから1日ぶんを切り出したときだけは**別の予定**なので新しいuidになる。
    uidが変わらなくなったことで、実績のメモ・「やった」印など**予定のuidで結びついている記録も編集後に残る**。
- **動機づけ面談（MI）**: 動機のものさし、決断バランス（両価性）、価値、チェンジトーク・ジャーナル、変化の計画
- **if-then**: 「もし〇〇なら〇〇する」プランニング

### 4-5. 減酒・飲酒コントロール支援
- 踏みとどまりボタン＋クールオフ
- **次に飲みに行く予定（計画飲酒）**（`nextDrinkCard`。アルコールタブ＋ウィジェット）。設定した日は予定タブ「日」の
  「📌 終日…」欄に「🍺 外に飲みに行く予定」を表示。
  - **📅 予定に追加**（`openNextDrinkAsEvent`）：その日の**19:00〜22:00**・タイトル「🍺 ＜メモ＞」（メモが無ければ「🍺 飲みに行く」）で
    予定の追加画面を開く。時間・場所・色などはふつうの予定と同じに直せる。過ぎた予定には出さない。
  - **🔗 既存の予定から選ぶ**（`renderNdPickModal`）：すでに入れてある予定を、この飲み予定として結びつける
    （`linkNextDrinkEvent`＝予定に`ndId`を付け、飲み予定の日付もその予定の日に合わせる）。候補（`ndCandidateEvents`）は
    **今日以降・180日以内の単発の予定**で、すでに結びついているものは除く。繰り返しの予定は1回ぶんを指せないので候補にしない。
  - 結びついた予定の行の**「解除」**で、予定は残したまま結びつきだけ外せる（`unlinkNextDrinkEvent`）。
  - **ウィジェットとアルコールタブで同じものを出す**（`nextDrinkCard`）。作る操作も、作った／結びつけた予定の行も、
    どちらの画面でも同じに見える（作ったものが見えないのは分かりにくいため、ウィジェットだけ簡素にする作りは取りやめた）。
  - **作ったあとはボタンを消して、その予定を出す**（タップでその日の予定へ）。予定を消せばまた「予定に追加」に戻る。
    結びつきは予定側の`ndId`（飲み予定の`id`）で持つ（`nextDrinkEvents`）。予定を作り直すと新しいidになるので、前の予定とは混ざらない。
  - **飲み予定の日を変えたら「次の機会」として立て直す**：前の日に作った予定は**動かさず・消さずにそのまま残し**、
    結びつき（`id`）だけを切る。だから新しい日にはまた「📅 予定に追加」が出て、別の予定を作れる。
    飲みに行く機会そのものが別なので、記録として前の予定を残すほうが自然（日付をずらすと前の予定が消えてしまうため変更）。
  - **予定を動かしたときだけ日付を合わせる**：その予定を別の日に動かせば、飲み予定の日付もその日になる（`syncNextDrinkFromEvent`）。
    同じ機会の日程変更なので予定は失われない。同期中は入口で止めて呼び合いを防ぐ。
- 飲みに行きたくなる引き金の記録
- 純アルコール上限ゲージ（日/週の上限設定）
- BACプロファイル（体重・性別）
- 休肝日連続・マイルストーン
- 飲まない夜のルーティン（夜の習慣の置き換えチェック）
- ※「なりたい自分・減酒する理由」カードは、私の宣言/定期ルーティンと重複のため現在は非表示（コードは保持）

### 4-6. 就寝リズム（夜更かし防止）
- 目標就寝時刻（直/休日別・シフト別・パターン予定連動）
- 寝支度（ナイトルーティン）スイッチと連動、開始時刻編集可
  - **「⏰ 寝支度が◯分 遅れ」は、まだ寝支度を始めていないときだけ表示**する。始めた後は
    「🛏 寝支度スタート h:mm ／ 目標 h:mm・◯分遅れ（早め・ちょうど）」と、**実際に始めた時刻で決まる固定のズレ**を出す
    （以前は現在時刻との差を出していたため、アプリに戻るたび遅れが増えていた）
- 今夜のカウントダウン（目標睡眠の指定時間前から表示）
- カウントダウン中はウィジェット最上部に固定表示（寝支度を押すまで／取消で再表示）
- 0時をまたいでも前日の記録として正しく扱う（夜フレーム処理）
- **🌙 睡眠前モード**: 「寝支度を始める」を押すと全画面の専用画面に切替（`state.sleepMode`）。
  **睡眠前モードに入ること＝寝支度を始めること**なので、入口カードからでも**モードの切り替えからでも**
  その時刻を`windDownAt`に記録する（`startWindDown()`に一本化）。すでに記録があれば**上書きしない**ので、
  開き直しても開始時刻はずれない。👁プレビューでは記録しない。
  - **入口はすべて `openLifeMode('sleepprep')` に一本化**。就寝リズムカードの「🛏 寝支度を始める」
    「🌙 睡眠前モードを開く」・入口カードのボタンも、画面を開くだけでなく**生活モードを🌙睡眠前に切り替える**
    （以前は画面だけが変わり、モードは前のまま＝アプリに戻ると元のモードに戻っていた）。
    すでに🌙睡眠前なら切り替えは起こらず、経過時間もそのまま引き継ぐ。
  ① 減光＋暖色の静かな画面（大きな時計・就寝目標カウントダウン）
  ② 夜のルーティン（ナイトルーティン群＋夜項目）に集中できるチェックリスト
  ③「就寝した」で就寝時刻（`bedTime`）を睡眠記録に直接保存し、モード解除
  - 解除条件：「就寝した」／「アプリに戻る」／「寝支度を取り消す」。就寝リズムカードの
    「🌙 睡眠前モードを開く」から再入場可。時計は起動中1分ごとに更新。
  - **開始時刻はこの画面からも直せる**：「🛏 寝支度スタート h:mm」の横の「✏️ 時刻」で時刻入力＋保存。
    就寝リズムカードの編集と同じ保存処理（`saveWindDownTime(H,M)`／0〜11時の指定は**その夜の翌日**扱い）を
    使うので、どちらから直しても結果は同じ。👁プレビューでは編集ボタンを出さない。
  - **🛏 寝支度スタート ↔ 🌙睡眠前モードの開始は必ず同じ時刻**（片方を直すともう片方も動く）。
    「睡眠前モードに入ること＝寝支度を始めること」なので、ズレたままにできない。
    - モードの開始を直した（`setModeSessionTime(sess,'start',...)`／モードの挿入）→ `syncWindDownFromMode`が
      その夜の`windDownAt`を書き換える。**終了時刻を直しても寝支度は動かない**（開始だけが寝支度）
    - 寝支度の時刻を直した（`saveWindDownTime`）→ `syncModeFromWindDown`がその夜の睡眠前モードのセッションの開始を動かす。
      **先にモードを動かせるか確かめてから保存する**ので、前後のモードとぶつかって動かせないときは
      **どちらも変えない**（`saveWindDownTime`は`null`を返し、理由をトーストで出し、編集欄は開いたまま＝別の時刻を入れ直せる）。
      2つの時刻がズレるくらいなら**合っていることを優先**する。直したいときは先に睡眠前モードの時間を直す。
      進行中の睡眠前モードは未来へ動かせないので、**まだ来ていない時刻（深夜0時台など）も同じ理由で断る**
    - どちらから入っても呼び合いが止まるよう`_windSyncing`で1回だけに制限する。
      「その時刻がどの夜のものか」は`nightDayOfTs(ts)`（正午より前は前日の夜）で決め、`sleepprepSessionForNight(nd)`で対象を探す
  - **入口カード**：寝支度中〜就寝記録までウィジェットに常設。まだアプリに戻っていなければ
    **一番上**、一度「アプリに戻る」を押した後は**一番下**（`smReturned` フラグ）。就寝(`bedTime`)を
    記録すると消える。
  - **寝る前にやることリストのルール**：**「表示するモード」を🌙睡眠前にした項目**を、
    **ルーティンと同じ並び順**で表示（`routineItemsForMode("sleepprep")`。既定は夜の項目）。
- **🛏 夜ごとの睡眠**（データの組み立て。表示は**分析→睡眠**＝4-7）：記録したものを「どんな夜だったか」として並べるための下ごしらえ。
  - **1夜の単位＝就寝する日 `ds`**。就寝は`ds`の`bedTime`、起床は**翌日**の`wakeTime`、中途覚醒は
    **目が覚めた朝**＝翌日の`awakenings`（予定タブの睡眠実績とまったく同じ数え方）。`sleepLogNight(ds)`が1夜ぶんを組み立てる。
    正午前の就寝（0:30など）は**その夜の翌日**として扱う。就寝・起床が無くても**質・メモ・夢があれば**その夜は記録ありとみなす（`has`）。
  - **帯**（`sleepLogBand`）：物差しは`ds`の**18:00〜翌12:00**。就寝→起床を睡眠の実績色で塗り、**中途覚醒の分は抜く**。
    🛏寝支度＝紫の縦線、🎯就寝目標＝黄の点線、0時＝細い縦線。**就寝か起床の片方しか無い夜は8時間と仮定した薄い点線の帯**で出す。
  - **1行**（`sleepNightRow(g,tol)`）：`🛏 寝支度｜🌙 就寝（就寝目標とのズレ±分）｜🌅 起床｜😪 中途覚醒 回数と合計｜💤 その日の昼寝`、
    右端に**実睡眠**（`getNightSleepHours`と同じ値＝中途覚醒を差し引き。7h以上=緑／5h以下=赤）。
    ズレが許容（`sleepTol`）を超える夜は内訳を黄色にする。各行に眠りの質の選択UI（小）を置き、**つけ忘れた夜をここから直接埋められる**。
  - **行をタップすると「起床した朝（翌日 `nd`）」の日記→睡眠へ移動**する（`sleepNightJumpDS`）。
    起床・眠りの質・夢・中途覚醒はその朝の日付に記録されるため、就寝日へ移動すると別の夜の記録が開いてしまう。
    日付の下に小さく`→M/Dの朝`を出して行き先を明示する。**まだ朝が来ていない「今夜」の行だけは就寝日にとどまる**（未来の日付へ飛ばさない）。
  - **まとめ**は`sleepLogSummary(nights)`（平均就寝・平均起床・平均実睡眠・平均の質・目標±◯分に収まった夜数。記録のある夜だけで計算）。
  - ※**日記→睡眠タブの一覧（旧「🛏 睡眠ログ」）は廃止**し、分析→睡眠の履歴に一本化した（同じものが2か所にあり重複するため）。
    日記→睡眠には**「🛏 睡眠の履歴を見る（分析）→」の導線**だけを残す（`goToSleepHistory`）。入力欄（就寝・起床・質・メモ・夢・中途覚醒・昼寝）はそのまま。
- **🛌 眠りの質（主観・1〜5）とひとことメモ**（`sleepQuality` / `sleepNote`）：センサーによる推定ではなく**本人の実感**を1タップで残す。
  - **記録先は「目が覚めた朝」の日付**（中途覚醒・起床時刻と同じ規則）。睡眠タブの「🛌 眠りの質（前夜）」＝表示中の日の朝までの夜の評価。
  - 5段階は`SLEEP_QUALS`（😵最悪／😪よくない／😐ふつう／🙂よい／😊ぐっすり）。**同じ評価をもう一度押すと取り消し**（`setSleepQual`）。
    選択UIは`sleepQualPicker(ds,sm)`の1本で、睡眠タブ（大）と**睡眠の履歴の各行（小）**の両方に出す＝**つけ忘れた夜を履歴から直接埋められる**
    （行タップの日付移動とは分けるため`stopPropagation`）。
  - メモは`setSleepNote`で前後の空白を落として保存（空なら`null`）。履歴の行にはメモがあればメモ、無ければ評価名を出す。
  - **💭 夢**（`dream`）も同じ夜（＝目が覚めた朝）に記録する。**😴睡眠モードの画面**にも眠りの質と夢の入力欄を置き、
    目が覚めたその場で残せるようにした（`dreamField`。保存先は日記→睡眠とまったく同じなので、あとから直せる）。
    👁プレビューでは入力欄を出さない。履歴の行にはメモ（📝）優先で、無ければ夢（💭）を出す。
  - **既存の分析にそのまま乗る**：モード×コンディションの指標（`MODE_METRICS`の`sleepQ`・5点満点）、
    アルコールの影響（`alcoholImpactStats`の`drinkNextQual`/`soberNextQual`＝飲んだ翌朝と休肝日翌朝の質の比較）、
    朝のサマリーとAIに渡すコンテキスト。

- **🧭 セルフモード（＝ライフモードのケアモード）**: 睡眠前モードを汎用化した全画面の状態切り替え支援（`state.selfMode`）。
  独立ランチャーは廃止し、**現在モードバー→切替画面**から起動する1系統に統合。飲み・朝・睡眠前は**生活モード**へ昇格し（上記ライフモード参照）、
  切替画面の「🩹ケア」は**落ち込み・たかぶり・エロ・さみしい**の4つ。
  **ケアも生活モードとして時間を計測する**（`DEFAULT_LIFE_MODES`に`care:true`で登録。下記ライフモード参照）。共通枠として、**いまの体調を記録するカード**（下記）と
  呼吸ガイド（4-7-8、円のアニメーション）を各モードで使える。終了時に
  `_selfmodelog`（開始時刻・モード・所要秒・前後の気分・メモ、末尾300件）へ記録し、**分析タブの「🩹 ケアの履歴」**（`renderSelfLogCard`）で確認できる。
  期間は**モード別の時間と同じ1日／1週間／1ヶ月**で、‹ ›で**過去へもさかのぼれる**（`state.selfLogRange`／`modeStatPeriod(range,"selfLog")`。
  ナビは`periodRangeTabs`/`periodNavRow`で共通化。オフセットはモード別の時間とは独立）。
  **落ち込み・たかぶり・エロ・さみしいの4つを常に並べ**（記録が無ければ「記録なし」と薄く表示）、
  回数・合計/平均の所要時間・心の調子のbefore→after平均差、期間の合計、期間内のメモ（最新5件）を出す。
  🍶飲み・☀️朝も同じ画面の仕組みで`_selfmodelog`に残るが、**ケアではないのでこのカードには出さない**（`isCareMode`で絞り込み）。
  - **🍶 飲みモード**：経過時間・当日の純アルコール量(`pureAlcoholG`／1日上限併記)・杯数・**💧水分摂取量(L)**を表示。
    **🍺 飲んだものを記録**（「＋ 飲み物を追加」で**アルコールタブと同じ追加モーダル**＝種類・量・**アルコール度数(%)**・時刻を`renderAddDrinkModal`で入力（**量(ml)は直接入力**。種類を選ぶとその種類の目安量が入る。未入力・0では保存不可）。セルフモードの上に重ねて表示。記録済み一覧＝種類・度数・時刻・純ｱﾙｺｰﾙ表示＋各行削除）、
    各行に**🍵飲み終わり時刻の記録/取消**（`recordEnd`/`clearEnd`＝`endHour`/`endMin`）。「今日はここまで」で最後の1杯の飲み終わりを自動記録して終了。
    「次の1杯まで20分あける」ディレイタイマー、「水を1杯飲んだ」(+0.2L)、水分の目安（純アルコールと同量ペース）、
    減らしたい理由（`lifeVision`）の再確認、編集可能チェック項目、「今日はここまで（記録して終了）」。
  - **🌧 落ち込みモード**：ねぎらいメッセージ＋深呼吸／誰かに連絡（つながりタブへ）／5分だけ動く（タイマー）／
    書き出す（メモ）／お酒に逃げない注意＋編集可能チェック項目。気分を記録して終了。
  - **⚡ たかぶりモード**：呼吸で落ち着く＋24時間ルール（編集可能チェック項目でセルフ問診）＋
    if-thenプランへの導線。**「🔀 if-thenプランを見る」は見に行くだけなのでモードを終わらせない**
    （`leaveSelfScreen()`＝画面を閉じるだけ。ケアの記録も残さず、生活モードの時間も続く）。
    ヘッダーの現在モードバーから戻れば、書きかけのメモ・チェック・経過時間もそのまま続きになる
    （以前は`exitSelfMode(true)`を呼んでいたため、見に行くだけでモードが終わり元のモードへ戻っていた）。
    行動そのものへ移るボタン（🫂誰かに連絡する・🎮別のことに切り替える・🍵飲み終わり）は今までどおりモードを終える。
  - **🔥 エロモード**：性的な衝動を「波」として扱い、通り過ぎるまでを支える（飲酒のブレーキと同じ考え方）。
    **⏳10分だけ待つ**ディレイタイマー（カウントダウン表示・途中でやめる可）／深呼吸／🚿体を動かす・シャワー（5分タイマー）／
    **🎮別のことに切り替える**（🎮趣味モードへ生活モードごと切替）／きっかけ・状況のメモ＋編集可能チェック項目。記録して終了。
    **💧 射精の記録**もここから扱える（`ejacSectionForMode`）：「＋ 記録する」で**生活記録と同じ記録モーダル**（`renderAddEjacModal`）を
    モードの上に重ねて開き、その日の**一覧・編集・削除**もできる。データは日記→生活記録の「💧 射精」と同一（`dailies[].ejaculations`）。
  - **🕯 さみしいモード**：さみしさを「つながりたいサイン」として扱う。🫂誰かに連絡する（達成タブ→つながり）／深呼吸／
    🚶5分だけ外の空気を吸う（タイマー）／気持ちの書き出し＋「お酒でまぎらわせない」注意＋編集可能チェック項目。記録して終了。
  - **☀️ 朝モード**：起床チェック＝**「表示するモード」を☀️朝にした項目**（`routineItemsForMode("morning")`。既定はモーニングルーティンの各項目）を
    チェックリスト表示（タイマー式なら時間・内容も記録可）＋今日の目標件数。
    起床時刻は通常😴睡眠モードの「🌅起床した」で記録済みのため、記録があれば「🌅 起床 h:mm」と**結果のみ**表示し、**未記録のときだけ記録ボタン**を出す（睡眠モードを経由せず入った場合の保険）。
  - **チェック項目の出どころ**：🌙睡眠前・☀️朝ともに、ルーティン設定の**項目ごとの「表示するモード」**で決まる（既定＝夜の項目／朝の項目）。
    飲み・落ち込み・たかぶり・エロ・さみしいの5モードは**モード内の「✏️ 編集」で自分で追加・改名・削除・初期化**できる独自チェック項目（`_selfchecks`）。
  - **🩺 いまの体調を記録（体調記録と同期）**：**すべてのモード画面に同じものを表示する**（`appendModeCondition`）。
    生活モード画面・セルフモード画面（飲み/朝/ケア4種）・🌙睡眠前モードのどれを開いても同じ内容・同じ保存先で、
    配色だけ画面に合わせて差し替える（モード画面＝暖色の既定、生活モード画面＝`SELF_COND_PAL_LIFE`、ウィジェット＝`SELF_COND_PAL_APP`）。
    **体調記録とまったく同じ項目**を入力できる
    （`selfConditionCard`）：**💪体の調子（体調スコア1〜10）／🧠心の調子（メンタル1〜10）／🍺飲酒欲求（任意1〜10）／
    体調状態（症状・複数可）／メモ**。保存先も体調記録と同一（`setLog(selfCondDay(), selfCondHour())`／飲酒欲求は日次`cravings[時刻]`）で、
    **記録先の日付は基準日(`activeDay`)ではなく実際のカレンダー日付（`selfCondDay()`＝`todayStr()`）**を使う。
    体調記録は「カレンダー日付＋その日の0〜23時」で持つため、深夜0時すぎに基準日（前日を指すことがある）へ書くと
    24時間前の同じ時刻に入ってしまうため。
    入力中は下書き（`state.selfCond`）に保持し、**「🩺 体調を記録する」ボタン**で体調記録へ保存する（未入力の間はボタン無効）。
    保存先が体調記録と同一なので、モードで入れた値は体調記録タブに、体調記録で直した値はモード画面に反映される。
    **その時間にすでに記録があると入力欄は表示せず**、「🩺 ◯時の体調は記録済み」の1行（記録内容の要約つき）に切り替わる（`selfCondRecorded`）。
    修正したいときは行の「✏️ 修正」で入力欄を開き直せる（`state.selfCondEdit`）。体調記録タブで入れた時間も同じく非表示になる。
    「記録して終了」時は、どのモードで記録したか分かるようにメモへモード名（＋モード内メモ）だけを追記する。
    これにより**週次ふりかえり・AI分析から参照可能**になる。
    `_selfmodelog`のbefore/afterは、モード開始時の心の調子と終了時の心の調子（1〜10）を記録する。
  - 解除：「閉じる」（記録なし）／各モードの「記録して終了」。時計・タイマーは起動中1秒ごとに更新。
  - **開き直しても状態を引き継ぐ**：すでに進行中のモードを開き直した場合（`openLifeMode`が判定して`enterSelfMode(key,resume)`へ渡す）、
    経過時間・チェック・待ちタイマー（次の1杯まで20分等）・メモ・気分をリセットしない。経過時間の起点は`selfModeStartAt()`が
    **生活モードのセッション開始時刻（`modeLog`・永続）**を使うため、アプリを開き直しても正しい経過が出る。
    別のモードへ切り替えた場合は毎回リセット（=新しい介入の開始）。ケアモードも生活モードなので、開き直しは経過を引き継ぐ。

- **基準日（`activeDay()`）**: モードでの記録先の日付。**就寝(`bedTime`)を記録するまでは前日扱い**とし、日付が変わっても基準日を変えない（深夜の飲酒・記録が“その夜の分”としてまとまる）。就寝を記録すると翌日へ切り替わる。就寝を記録し忘れた場合の安全弁として、**フォールバック時刻（既定12:00・`dayCutoffH`）以降は当日**。飲みモードでは前日基準で記録し、実時刻が翌日なら`startNextDay`を付与（純アルコール総量は前夜の日に集計、タイムライン表示は実時刻＝翌日側）。各モード（飲み/食事/自己投資/休息/朝など）の記録・気分連携がこの基準日を使う。フォールバック時刻は切替画面の設定で変更可（`_nudgecfg.fallbackH`）。

- **🕒 ライフモード**: 「常に1つの生活モードに属する」時間トラッキング＋行動サポート（`state.lifeModes`／`state.modeLog`）。
  切り替えると前のモードを終了して**タイムライン（`{key,start,end}`のセッション列）**を作り、1日/週のモード別時間を集計できる。
  - **モード別時間の集計（分析→日次ログ）**: `renderModeStatCard()`が**モード別の時間**を帯グラフ＋一覧（アイコン・名称・時間・構成比）で表示。範囲は**1日／1週間／1ヶ月**を切替（`state.modeStatRange`）。
    - **どの範囲でも期間を1つずつさかのぼれる**（`modeStatPeriod(rangeKey)`が対象期間・前期間・見出し・前後の移動をまとめて返す）：
      **1日**＝前日/翌日（`state.modeStatDay`）、**1週間**＝月〜日の暦週（`state.modeStatWeekOff`）、**1ヶ月**＝暦月（`state.modeStatMonthOff`）。
      ‹ ›で移動し、未来へは進めない。現在の期間には「今週／今月／今日」と表示し、さかのぼった時は「今週に戻る」等のボタンを出す。
      期間内の**未来の日は含めない**ので、今週・今月でも「1日あたり平均」は経過日数で割られる。記録が無い期間でもナビは残る。
      集計は`modeTotalsForDay(ds)`／`modeTotalsForRange(dsList)`（`modeLog`を各日0〜24時に切り出して合算）。
    - **モード名タップで「何時から何時まで」を展開**（`state.modeStatOpenKey`／`modeSessionsForRange(dsList,key)`／`modeSessionListBox`）：
      その期間にそのモードだった**時間帯を1本ずつ古い順**に出す（開始〜終了・タグ・長さ、末尾に「◯回・合計 ◯時間」）。
      行頭の ▸/▾ で開閉が分かる。日をまたいだセッションは**1本のまま**扱い、期間の端で切れたものだけ「（前から）」「（先へ）」を添える。
      進行中のモードは終了を「いま」と表示。**1日**表示では日付を省き、1週間／1ヶ月では日付を添える。
      合計は一覧の集計と一致する。件数が多いときは新しい30件を表示し「古い◯件は省略」と出す。
      **時間帯をタップすると、その記録の時間を直せる**（下記「モードの時間を直す」。期間の端で切れていても直すのは元のセッション）。
    - **前期間との比較**：各モードに「↑↓ 前日比／前週比／前月比 ◯時間」を表示（1つ前の同じ期間との差）。
    - **🏷 タグ別の時間**：タグを使うモード（`MODE_TAG_DEFAULTS`。現在は🫂交流）は、一覧のそのモードの下に
      **タグ別の時間・構成比**を出す（`modeTagTotalsForDay/ForRange`。タグ無しの時間は「（タグなし）」）。
    - **🎯 モード別の目標時間**：モードごとに「1日の目標時間」を**⬆これ以上／⬇これ以下の両方**で設定できる
      （設定→モードの設定。`state.modeGoals`／`_modegoals`／`modeGoalOf`／`setModeGoal`。片方だけでも可）。
      例：トレーニングは30分**以上**、仕事は8時間**以下**、食事は45分以上〜1時間30分以下。
      保存形は`{min,max}`（分）。**古い形（数値ひとつ＝これ以上）もそのまま読める**ので、既存の設定は変換不要。
      集計には**達成ゲージ**を出し、帯の上に**⬆以上（緑）・⬇以下（赤）の目盛り**を立てる。
      判定は`modeGoalStatus()`＝「あと◯◯」（以上に足りない）／「◯◯ 超過」（以下を超えた）／「✓ 達成」。
      目標があるのに期間中まったく記録が無いモードは「記録なし」として一覧に出す（**「以上」がある場合だけ**。
      「以下」だけなら0分は達成なので出さない）。AIへの受け渡しにも「目標◯h以上・◯h以下/日・達成/未達」を含める。
    - **🔎 モード時間と体調の関係（分析→日次ログ）**：`renderModeInsightCard()`が直近30日について、各モードの**時間が多い日（上位半分）と少ない日（下位半分）**で **体調／心の調子／その夜の睡眠／純アルコール** の平均を比較し、差が大きい順に提示（`modeConditionInsights`）。日数が少ない・差が小さい場合は表示しない。因果ではなく傾向である旨を明記。
    - ※かつて週次分析タブにあった`renderModeWeekCard()`（その週のモード別時間）は、上の「🕒 モード別の時間」で**1週間**を選べば同じものが見られるため**廃止**した。
    - **AIへの受け渡し**：`buildModeContext(days)`がモード別の合計・1日平均・構成比・目標達成率・日別内訳をテキスト化し、**✨AI総合分析**（直近14日）と**💬AIに質問**（直近30日＋1日ごとの記録行にも`モード[...]`）のコンテキストに含める。
    - **📅 日別（24時間の使い方）**：範囲内の各日を1行で表示し、**0〜24時のどの時間帯にどのモードだったか**を色帯で見せる（`modeSegmentsForDay(ds)`。予定タブの実績列の色帯と共通）。行タップでその日の**モード別内訳（時間・構成比）＋切替回数・記録時間・未記録時間**を展開（`state.modeStatOpenDay`）。
  - **実績タイムラインのモード色**: 予定タブの**実績（右）列**に、その時間に選択していたモードの色を**左右両端の色帯**（幅6px）＋ごく薄い背景で表示（`modeLog`を当日0〜24時に切り出し）。実績ブロックの**上**（`z-index:4`・`pointer-events:none`）に重ねるので、**実績が多くてもモード色が常に見える**。中央は透明でブロックを隠さず、タップは下の実績ブロック／空きレイヤーへ通る。帯には識別用のアイコンチップ（半透明の下地付き）を表示。帯の左右位置・幅は**実績列の幅（いまの前後で変わる／4-1参照）に追従**する。
  - **生活モード（常態・編集可能）**：🏠生活／🍚食事・🛋休息／📚自己投資・🏋️トレーニング／🍶飲み・🫂交流／🏢仕事・💻副業／🎮趣味・☀️朝／🌙睡眠前・😴睡眠 の13種（`DEFAULT_LIFE_MODES`）。
    **配列の順がそのまま切替画面の並び（2列）**で、**先頭の🏠生活だけ1行を使って一番上**に出る。
    保存済みの一覧はこの並びに揃える移行を**1回だけ**行う（`migrateModeOrder`／`MODE_ORDER_VER`・`_modeorder`。既定に無い自作モードは末尾に残す）。
    先頭の **🏠生活**（`key:'life'`）が「いつもの生活」を表す**既定モードの初期値**で、定期ルーティンの表示先でもある。
    かつて旧版の「＋モード追加」で自作した**ラベルが「生活」のモード**は、起動時に`migrateSeikatsuMode()`が正式な`life`へ統合する（色・並び順は引き継ぎ、`modeLog`・`_defaultmode`・`_automode`の直近キー・ルーティンの`mode`・モード別目標時間の参照をすべて付け替えるので記録は失われない）。この移行は`_modelog`／`_modegoals`／`_defaultmode`の読み込み**後**に実行する。
    🏋️トレーニングは日記→トレーニングへ、🫂交流は達成→つながりへの導線。
    このうち **☀️朝・🍶飲み・🌙睡眠前** は時間を占有する生活モードだが、開くと従来の**リッチ画面**（朝＝起床支援、飲み＝飲酒ブレーキ＋記録、睡眠前＝寝支度）を表示する（`screen`で分岐）。
    ☀️朝・🍶飲みの「記録して終了」でそのモード時間も終了、🌙睡眠前の「就寝した」で😴睡眠へ引き継ぐ。
    - **終了時の自動遷移（`MODE_AFTER_FINISH`／`finishLifeMode`）**：😴睡眠モードの「🌅起床した（睡眠を終了）」で`wakeTime`を記録し**☀️朝モードへ自動切替**（朝の画面を開く）。
      🍶飲みモードの「🛑今日はここまで」で飲み終わりを記録し**🛋休息モードへ自動切替**。その他のモードは従来どおり終了して既定モードへ戻る。
    **ヘッダーの日付の上に「今のモード」帯を常時表示**（`headerModeStrip`・全タブ／ウィジェット共通）：モード名＋経過時間を表示し、タップで**そのモードの画面に直行**、右の「🔀」で切替画面。経過は`lifeTick`が毎秒更新（`#hdr-mode-sub`）。ヘッダーは作り直さない実装のため`updateHeaderDynamic()`で帯だけ差し替えて最新状態を反映する。モードの各種設定は**設定タブの「🕒 セルフモード・切り替えの設定」**に集約（色・既定モード・自動切替・基準日）。**通知（切り替え提案）の設定は「🔔 通知」に分離**した。
  - **常に1つのモードに属する（未選択なし）**：進行中セッションが無ければ`ensureActiveMode()`が**既定モード**を自動開始（初期値は🏠生活。切替画面の色編集画面で変更可・`_defaultmode`）。
  - **画面全体のイメージカラー**：ウィジェット/タブの背景を現在モードの色で淡くティント（`document.body`の背景グラデーション）。全画面モードは各自の背景。
    さらに**アプリのアクセント色**（CSS変数`--mode`／`--mode-soft`）を現在モードの色に統一：タブの選択色、主要ボタン(`.btn-primary`)、
    ヘッダーの日付/バージョン/設定ボタン、**各タブのサブタブ（選択中の文字色・下線）**、現在時刻行、取り消しバー等が追従（意味を持つ色＝成功緑・警告赤・飲水青やユーザーの予定色は保持）。
    さらに**ベース面**（カード`.card`・タブバー・ヘッダー・モーダル・区切り線）も`--surface`/`--surface-2`/`--border`（モード色を混ぜたダークトーン、`mixHex`）でモード色に寄せる。
  - **中央モーダル（`.modal-center`/`.modal-box`）は中身が画面より高くなってもスクロールできる**：
    `.modal-box`に`max-height:calc(100dvh - 28px)`（未対応環境は`100vh`）と`overflow-y:auto`、`.modal-center`側にも`overflow-y:auto`と上下の余白。
    以前は高さ制限が無く、中身が増えた画面（目標の詳細など）は**上下に画面外へはみ出して触れなくなっていた**。
    個別に`maxHeight`/`overflowY`をインラインで指定しているモーダルはそちらが優先される。
    さらに**文字色**もモード色にわずかに寄せる（CSS変数`--body-text`/`--text-1`〜`--text-4`/`--dim`）。
    濃い文字ほど弱く・淡い文字ほど強く混ぜる（8%〜24%）ことで読みやすさを保ち、カード上の本文コントラストは
    WCAG AAA（7:1）を上回る水準を維持する。意味を持つ色（成功緑・警告オレンジ・危険赤など）は対象外。
    加えて、コード内で直接指定された**細かいサブパネルの暗い面色・文字色**（面：`#1e293b`/`#0f172a`/`#111827`/`#0a1120`/`#1e3a5f`、文字：`#f1f5f9`/`#e2e8f0`/`#cbd5e1`/`#94a3b8`/`#64748b`/`#475569`/`#334155`）は、描画後に`tintSurfaces()`がDOMを走査して対応するモード色トーン（`--surface`/`--surface-2`/`--border`相当）へ一括置換（`window.__tintMap`）。半透明（rgba）指定やcanvasの描画色は対象外なので、オーバーレイの透過やグラフ描画には影響しない。
    - **どのモードにどのルーティンを出すかは設定で選ぶ（項目ごと）**：ルーティン設定（設定→⚙ ルーティン設定）の各項目に**「表示するモード」**セレクトがあり、**ルーティン項目1件ずつ**に割り当てる（`todoItems[].mode`／`routineItemsForMode`）。選んだモードの画面にその項目が並び、**チェック・▶開始/⏹停止の時間記録・セッションの内容入力**ができる。「なし」を選ぶとどのモードにも出さない。**すべてのモード**（仕事・副業・自己投資・トレーニング・趣味・交流・食事・休息・朝・睡眠前・睡眠など）が対象。
      モード画面では、割り当てられた項目を**所属するまとめごとのカード**に分けて表示（`routineModeSections`。まとめ未所属は「🔁 その他のルーティン」）。同じまとめの項目でも**別々のモードに振り分けられる**。
      既定の割り当ては従来の表示とほぼ同じ（モーニング各項目→朝／朝食・昼食・夕食→食事／夜項目→睡眠前／インプット・アウトプット→自己投資／帰宅→仕事／トレーニング→トレーニング）。既存データには`migrateRoutineModes()`が、その項目が属するまとめの既定モード（`GROUP_DEFAULT_MODE`）から補完。
    - 🏢仕事＝時間記録＋割り当てたルーティン（既定は**帰宅**）／💻副業＝**時間記録のみ**（画面は経過＋切替。仕事中はスマホを触らない前提）
    - 📚自己投資＝既定で**インプット／アウトプット**の項目が割り当て済み。項目がタイマー式なら**▶開始/⏹停止で時間を記録**（既存のルーティン記録`setTodo`に直結・グループ合計時間を表示）、チェック式は✓トグル
    - 🎮趣味＝「楽しむ時間」の肯定＋時間記録／🍚食事＝割り当てた項目（既定は**朝食・昼食・夕食**の各項目）を表示し、それぞれを**▶開始/⏹停止で時間記録**（チェック式項目も食事モードでは計測扱い＝`startTime`/`endTime`＋`checked`を既存ルーティン記録`setTodo`に保存、グループ合計時間を表示）＋**🍽食べたものを記録**（食事モーダル、`foods`）＋飲水
    - モードのルーティン一覧は**すべて既存のルーティン/生活記録に連動**（チェック・タイマーの記録が通常の実績・タイムラインにも反映）
    - **内容メモはルーティン画面と完全同期**：マルチ計測項目（インプット/アウトプット/トレーニング等）は、モード画面でも**セッションごとの「内容」欄**（`何をしますか？`）を表示・編集できる。保存先はルーティン画面（タスク→ルーティン）と**同一の`sessions[].note`**なので、どちらで書いても常に同じ内容が出る（モード専用のメモ領域は持たない）。**複数セッション（複数回の計測）それぞれに内容**を書ける。書き込みは既存レコードを保持したまま更新（`excluded`等が消えない）。
    - 🛋休息＝呼吸(4-7-8)・飲水・回復＋時間記録／😴睡眠＝就寝〜起床の時間占有（「起床した」で`wakeTime`記録して終了）
  - **👁 見るだけ（プレビュー）**：切替画面のトグル（`state.peekArm`）をONにすると、タイルをタップしても
    **モードを切り替えずにその画面だけを確認**できる（`openLifeMode(key,{peek:true})`／`state.peekMode`）。
    生活モード画面・セルフモード画面（飲み・朝・ケア4種）・睡眠前モードのいずれにも対応し、画面の上部に
    プレビュー帯（`peekBanner`）で「いまのモードは◯◯のまま」＋**▶このモードに切り替える**／**一覧へ戻る**を表示。
    プレビュー中は`modeLog`に何も追加されず、経過時間は「—」。**モードを終える/切り替えるボタンは「あること」が分かるよう表示するが、押しても動かない**
    （`peekDisabled`＝`cloneNode`でイベントを外した薄い複製。押すと「プレビュー中：ボタンの確認だけです」と出る）。
    念のため`exitSelfMode()`もプレビュー中は記録しない。「一覧へ戻る」で切替画面に戻り、続けて他のモードも見られる。
  - **移行ボタンは行き先を表示する**（`nextModeAfter`／`nextModeLabel`）：「🌅 起床した（睡眠を終了 → ☀️朝モードへ）」
    「🛑 今日はここまで（記録して終了 → 🛋休息モードへ）」「😴 就寝した（記録 → 😴睡眠モードへ）」
    「✅ 記録して終了 → ◯◯モードへ」（ケアは直前にいたモード、それ以外は`MODE_AFTER_FINISH`→無ければ既定モード）。
    `MODE_AFTER_FINISH`に表示用として`sleepprep:"sleep"`を追加した。
  - **切替画面（フルスクリーン `state.modeSwitcher`）**：生活モードのタイル（`nonCareModes()`＝ケア以外）＋🩹ケア欄（**落ち込み・たかぶり・エロ・さみしい**。進行中は色付きで「●」表示）＋「🎨 モードの色を変更」（**色のみ**変更。追加・改名・削除は無し）。
  - **各モード画面（`state.lifeScreen`）**：選択した生活モードの没入画面（経過時間・中身・別モードへ切替）。
  - **🩹 ケアの行（`careQuickRow`）**：**🏠生活モードと🍶飲みモードの画面の上部**（経過時間の下・体調記録より上）に、
    4つのケア（落ち込み・たかぶり・エロ・さみしい）を並べる。つらくなったときに切替画面まで戻らず、その場で立て直しに入れる。
    進行中のケアには●が付く。ケア画面自身と他のモードには出さない。プレビュー中は`peekDisabled`で見えるが押せない。
  - **🕒 モードの時間を直す（`openModeTimeEdit`／`renderModeTimeModal`／`state.modal="modeTime"`）**：
    入口は2つ。**①モード画面の「◯:◯◯ 開始 ✏️」**（切り替え忘れをその場で遡る。−15分/−30分/−1時間のボタン付き）と、
    **②分析→🕒モード別の時間→時間帯の一覧をタップ**（過去の修正）。どちらも同じモーダルを開く。
    `modeLog`は**隙間も重なりもない鎖**（切り替えのたびに前を同じ瞬間に閉じる）なので、
    **編集するのは境目ひとつ**＝開始を動かせば前のセッションの終わりも一緒に動く（`modeSessionBounds`／`setModeSessionTime`）。
    両側に最低1分残し、範囲外なら何も変えずに理由を出す。隣と離れている（アプリを使っていなかった空白）ときはその空白の中でだけ動かせる。
    入力した時刻は**動かせる範囲に収まる日付へ自動で解決**するので、0時をまたぐモードでも「前日にする」のような操作が要らない（`resolveModeTime`）。
    **進行中のセッションは終了時刻を変えられない**（開始のみ）。
    手で直したときだけ「🕘 最近の保存」に**🕒 モードの時間**として載り、1つずつ戻せる。
  - **🕒 モードの時間を挿入（`openModeInsert`／`renderModeInsertModal`／`insertModeSession`）**：
    日付・開始・終了・モードを選んで、**その時間帯をそのモードで塗り替える**。
    入口は**分析→🕒モード別の時間の「＋ 時間を挿入」**と、**モードの時間を直す画面の「＋ この時間の中に別のモードを挿入」**。
    これ1つで「記録し忘れた**空きを埋める**」「長いモードの**途中に差し込む**（元が前後に分かれる）」
    「またいでいる**両隣を縮める**」「まるごと入るものを**消す**」がまかなえる（鎖を壊さないよう重なりは新しい1本に譲る）。
    終わりが「いま」に届き、その後に何も残らないときだけ**その挿入が進行中**になる。
    **未来には入れられず**、1分未満は断る。隣が同じモードになったら**つなげる**（無駄な切り替えを残さない）。
    挿入前にどのモードが短くなるかを下見で表示。「最近の保存」に載るので戻せる。
  - **🗑 モードの記録を削除（`deleteModeSession`／`cleanupZeroModes`）**：押し間違いで入った短い記録を消せる。
    鎖を壊さないよう、**その時間は前のモードに含める**（前が無ければ次のモードの始まりを早める）。
    消したのが進行中なら**前のモードが進行中に戻る**。前後が同じモードになったらつなげる。**最後の1件は消せない**。
    入口は**モードの時間を直す画面の「🗑 この記録を削除（時間は ◯◯ に含める）」**（行き先を明記）と、
    **分析→🕒モード別の時間の「🗑 0分の記録を◯件まとめて削除」**（1分未満で終わっている記録がある時だけ出る・確認あり）。
  - **🩺 記録の点検（勝手には直さない）**（`modeLogProblems`／`applyModeLogFixes`／`renderModeCheckModal`）：
    **1日の合計が24時間を超えるのは、記録どうしが重なっているときだけ**（集計`modeTotalsForDay`はその日で切るため）。
    いちばん多い壊れ方は**「終わりのない記録（進行中）が2本以上」**＝終わり忘れで、そのぶんが毎日ずっと二重に数えられる。
    - **アプリは見つけて知らせるだけ**。起動時に点検してトーストで件数を出し（`state.modeLogWarn`）、
      分析→🕒モード別の時間に**⚠ 警告と「🩺 点検して直し方を選ぶ」**を出す（記録が無い日を見ていても出る）。
    - **直し方は1件ずつ本人が選ぶ**（`MODE_FIX_OPTS`）。既定（おすすめ）は先頭だが、選ばなければ何も変わらない。
      ｜**終わり忘れ**：次の記録の開始で終わりにする／この記録を削除する／そのままにする
      ｜**重なり**：後を優先（前を縮める）／前を優先（後を遅らせる）／そのままにする
      ｜**すっぽり重なり**：中の記録を削除する／外の記録を前後に分ける／そのままにする
      ｜**開始と終了の入れ替わり**：入れ替える／削除する／そのままにする
    - 重なりは**「ここまでで一番あとまで伸びている記録」**と比べる（すっぽり入った記録のうしろに、
      外側とまだ重なっている記録があるため）。終わり忘れ・入れ替わりはその問題として別に挙げ、二重に出さない。
    - 修復は`saveModeLog`を通るので**🕘 最近の保存から戻せる**。
  - **🔊 Alexa連携**（設定→🔊 Alexa連携・`renderAlexaPanel`）：**モードに入ったときに、そのモードに登録したURLを叩く**
    （`alexaFireMode`。`switchLifeMode`の最後から1か所だけで呼ぶので、切り替え方法によらず同じに動く）。
    Alexa側は「URL Routine Trigger」等のスキルでトリガーを作ると**ドアベル**として見えるので、
    それをAlexaルーティンの実行条件にする。例：🌙睡眠前モードで照明を落とす／🏋️トレーニングで音楽をかける。
    - 保存は`_alexa`（`{on, hooks:{モードkey:{url,auth,method}}, relay, relayUrl}`）。**https のURLだけ**を有効として扱う（`alexaUrlFor`）。
      古い形（`hooks`の値が文字列＝URLだけ）も読める（`alexaHookOf`）。
    - **送り方は2通り**（`alexaCall`）。モードごとに「テスト」ボタンを置く。
      - **そのまま叩く**：`fetch(url,{mode:"no-cors",keepalive:true})`。ブラウザからは**他サイトへ独自ヘッダーを送れない**ので
        Authorizationが要るトリガーには使えず、**結果も読めない**（届いたかはAlexa側で確かめる）。
      - **中継サーバ経由**（既定）：自分のサーバの`alexa.php`へ`{url,auth,method}`をPOSTし、**サーバが代わりに叩く**。
        ヘッダーを付けられ、**GET/POSTを選べ**、**成功/失敗が分かる**（失敗したらトーストで知らせる）。契約は API.md 参照。
        `auth`は**値だけなら`Authorization`**、**`X-API-Key: xxx`のように「見出し: 値」で書けばその見出し**で送る。
    - **全体のON/OFF**があり、OFFなら何もしない。**起動直後の自動復帰では鳴らさない**（`_appReady`。開くたびに鳴るのを防ぐ）。
    - 送るのはURLを開くことだけで、**記録は一切送らない**。URLは端末内にだけ保存する（バックアップには含める）。
    - ※「決めた時刻に鳴らす」はPWAだけでは不可（閉じている間は動けない）。サーバ側の定期実行が要る。
    進行中はこれから伸びるので「0分」に数えない。どちらも「最近の保存」に載るので戻せる。
    自動・手動の**切り替えそのものは履歴に載せない**が、`modeLog`は戻す対象（`UNDO_FIELDS`）なので、
    切り替えのたびに履歴の「前」の状態へ現在の`modeLog`を写して、**他の保存を戻してもモードの切替は巻き戻らない**ようにしている（`touchSnapBaseline`）。
  - **🏷 モードのタグ（`state.modeTags`／`_modetags`）**：モードのセッション（`modeLog`の各要素の`tag`）に「何の時間だったか」を残せる。
    現在は**🫂交流モード**で使い、モード画面のタグチップ（`modeTagPicker`。既定＝家族／友人／恋人／職場／オンライン。その場で追加・改名・削除・初期化でき、バックアップにも含む）から選ぶ。
    同じタグをもう一度押すと外れる。**別のタグに変えると、そこでセッションを区切って新しく計測を始める**ので、それまでの時間は前のタグのまま残る
    （開始1分未満のときは付け替えのみ）。集計は分析の「🕒 モード別の時間」にタグ別の内訳として出る。
  - **🩹 ケアモードも生活モード**（`care:true`／`isCareMode()`）：落ち込み・たかぶり・エロ・さみしいは`DEFAULT_LIFE_MODES`に含まれ、
    選ぶと`openLifeMode()`で**生活モードごと切り替わり`modeLog`に時間が記録される**（分析の「🕒 モード別の時間」「日別の色帯」、予定タブの実績のモード色にも出る）。
    ケア画面には「このモードの経過」を表示。**「記録して終了」で直前にいたケア以外のモードへ自動で戻る**（`prevNonCareSession()`／`finishLifeMode()`。
    戻り先の`auto`フラグも引き継ぐので「自動で入った仕事→帰宅ルーティン完了→休息」の判定は壊れない）。「閉じる」だけなら、そのモードのまま継続する。
    ケアは**既定モードにはできず**（`nonCareModes()`で選択肢から除外）、モード編集では色だけ変更でき「1日の目標時間」は出さない（🩹ケアのバッジを表示）。
  - **自動切替**（`_automode`・切替画面でON/OFF、既定ON）：**勤務シフトの時間帯**（`getPlanShiftBlock`）に入ると🏢仕事へ自動切替。
    勤務終了後は、**🏠帰宅ルーティン(gohome)が全て完了するまで仕事モードを維持**し、完了したら🛋休息へ切替（`gohomeComplete`。項目が無ければ即休息）。
    `autoModeKey(now)`が勤務中=`work`/その他=`null`を返し、`autoModeTick`が判定（render時＋60秒ごと）。自動で入った仕事セッションには`auto`フラグを付け、この維持・休息化の対象にする。
    **手動で切り替えた場合は次の境界まで優先**（区間の途中では自動は割り込まない／手動の仕事は帰宅ルーティン維持の対象外）。初回起動時は手動選択を潰さない。ケア/睡眠の全画面表示中は自動切替しない。
  - 時間の可視化グラフは今後追加予定。

- **🔔 セルフスイッチ・ナッジ**（切り替えの提案／`_nudges`・`_nudgecfg`・`_wakegoal`）：ウィジェット上部（現在モードバー直下）に、今の状態に合わせた切り替え提案バナーを表示。
  評価は`evalNudges(now)`が全ルールから最優先1件を返す（全画面表示中・ナッジOFF時は出さない）。各バナーは［切り替える］［あとで（30分スヌーズ）］［今日はしない（当日抑制）］。
  - **R1 夜更かし→睡眠前**：就寝目標(`sleepGoal`)の**設定した分だけ手前**から（既定＝就寝リズムと同じ`cdLeadMin`。`sleepNudgeLead()`）、今夜`bedTime`未記録
  - **R2 寝坊→朝**：**目標起床時刻（シフト別`_wakegoal`）**を過ぎて本日`wakeTime`未記録（起床目標〜+2時間）
  - **R3 飲み過ぎ**：**飲みモードが設定の分数**（既定90分）続いた（`currentModeSession`基準・上限超過なら文言変化）で「そろそろ切り上げませんか？」。［切り替える］で切替画面へ
  - **R4 気分チェック（夕方・既定18:00）**：バナー内に**モード画面とまったく同じ体調入力カード**（`selfConditionCard`。
    💪体の調子／🧠心の調子／🍺飲酒欲求／体調状態／メモ）を表示し、**「🩺 体調を記録する」で体調記録へ保存**する（絵文字5択は廃止）。
    保存先は体調記録と同一なので、体調記録タブとそのまま同期する。配色だけウィジェット用（`SELF_COND_PAL_APP`）に差し替えて表示。
    **その時間（`selfCondHour()`）にすでに体調の記録があればバナーを出さない**（体調記録タブやモード画面で入れた分も含む・`selfCondRecorded`）。
    記録後は心の調子が**4以下で🌧落ち込み／10で⚡たかぶり**モードへ切り替える（`afterMoodRecorded`。旧5択と同じしきい値）
  - **R5 食事→食事**：**予定タブに入れた食事の予定に連動**。予定（イベント/パターン/定期ルーティン）のうち食事（朝食/昼食/夕食のキーワード）の**予定時刻を過ぎて未記録**なら提案（予定時刻から**設定した分数**以内・既定120分）。`scheduledMealsForDay`／`currentMealDue`
  - **R6 停滞→切替**：趣味/休息が**設定した分数**（既定180分）以上連続
  - 優先度：R3＞R2＞R1＞R4＞R5＞R6。
  - **🔔 通知の設定はすべて設定画面の「🔔 通知」にまとめる**（`renderNotifyPanel`／アンカー`#notify-settings`）。
    ルールの定義は`NUDGE_DEFS`（id・アイコン・名前・説明・設定できる時間）1か所にまとめ、画面も判定も同じ表を見る。
    - **項目ごとのON/OFF**（`nudgeRules[id].on`・`nudgeOn(id)`）。全体OFF（`nudgesOff`）が優先し、OFF中は「項目をONにしても出ません」と明示する
    - **項目ごとの時間**（`nudgeNum(id)`／`setNudgeNum`で範囲に丸める）：就寝＝**目標の何分前から**（空欄＝就寝リズムに合わせる）、
      食事＝**予定時刻から何分以内**、飲み＝**何分続いたら**、停滞＝**何分続いたら**。気分チェックは**時刻**（`moodCheckH/M`）、起床は**シフト別の目標起床時刻**（`_wakegoal`）をこのパネル内で編集する
    - **🌙 通知を出さない時間帯**（`notifyQuiet{from,to}`・`inQuietHours()`。日またぎ可・`from===to`は無効）＝**端末通知だけ**鳴らさない（アプリ内バナーは出る）
    - **再通知の間隔**（`notifyRepeat`・既定30分）
    - モード設定（🕒 セルフモード）からは通知の項目が外れ、「🔔 通知の設定 → 開く」ボタン（`goToNotifySettings()`）で移動する。ナッジバナーの「⚙️ 通知」からも同じ場所へ飛べる
  - **アプリ内バナー**（表示中に確実）に加え、**端末通知**（ローカル通知）に対応（`_nudgecfg.notify`／設定「🔔 端末通知を有効にする」）。
    `nudgeNotifyTick`が60秒ごとに、**画面が非表示(`document.hidden`)のとき**だけ、有効な最優先ナッジを端末通知（`ServiceWorkerRegistration.showNotification`優先→`Notification`フォールバック、**同一ルールは`notifyRepeat`分に1回**、**静かな時間帯は鳴らさない**、気分チェックは除外）。
    通知タップで`sw.js`の`notificationclick`がアプリを前面化（`#widget`）。**サーバなしのPWAのため、アプリ起動中/バックグラウンド中のみのベストエフォート**（完全終了中は鳴らない・特にiOS）。確実な起床はOS標準アラーム併用を案内。

### 4-7. 分析
- サブタブは **日次・週次 / 🛏睡眠 / 🏋️トレーニング / 総合分析 / 🤖AI分析** の5つ。
  **総合分析は記録から自動計算する統計だけ**（体調スコアの推移・睡眠/飲酒/飲水と体調の相関・食材の相関など）。
  **AIを使うものは「AI分析」タブに集約**した（`renderAIHub`＝✨AI総合分析 →💬AIに質問 の順）。
  旧サブタブ名「AIに質問」は開いたときに「AI分析」へ自動で読み替える。
- **🛏 睡眠（履歴）**（`renderSleepHistory`）: 過去の夜をさかのぼって見る。**期間は7/14/30/90夜・1年**（`state.sleepHistDays`）。
  - 上に**平均就寝・平均起床・平均実睡眠・平均の質・記録した夜数**（`sleepLogSummary`）と、目標±◯分に収まった夜数。
  - **推移グラフ**（`miniBars`）は指標を切替（`state.sleepHistMetric`）：🛌実睡眠（0起点・7h以上緑/5h以下赤）／😊眠りの質（`sleepQualColor`）／
    🌙就寝時刻／🌅起床時刻（時刻は最小値の少し下を底にする）。棒が14本以下のときだけ値のラベルを出す。
  - 下は**夜ごとの記録**（`sleepNightRow`）。**記録のある夜だけ**を並べる（1年でも見渡せるように）。30夜を超えたら「🔽 のこり◯夜を表示」。
    **日記→睡眠タブにあった「🛏 睡眠ログ」は廃止**し、ここに一本化した（4-6参照）。
- **🏋️ トレーニング（履歴）**（`renderTrainHistory`）: 過去のトレーニングをさかのぼって見る。**期間は7/30/90日・1年**（`state.trainHistDays`）。
  - 1日ぶんは`trainDay(ds)`（計測セッション・種目・有酸素をまとめる）。**ボリューム＝重量×回数×セットの合計**。
    記録のある日だけを新しい順に集める（`trainDays(n)`）。まとめは`trainHistSummary`＝**行った日・計測した時間（12h50m形式）・総ボリューム・総セット・有酸素km**。
  - **推移グラフ**は🏋️ボリューム／⏱時間／🔁セット／🏃有酸素km を切替（`state.trainHistMetric`）。
  - **🏋️ 種目べつ**（`workoutsByName`）：期間内の種目を回数の多い順に。各行に**回数・セット・⭐自己ベスト・前回の内容と日付**。
    タップで展開（`state.trainHistEx`）すると**その種目の記録が全部**出る（重量の推移グラフ＋1件ずつの行）。行タップでその日の日記→トレーニングへ。
  - **📅 日ごとの記録**：日付・計測時間・種目数/セット・ボリューム・有酸素と、その日の種目/有酸素/セッションのメモを最大6件。
    行タップでその日の日記→トレーニングへ（`goToTrainingDay`）。20日を超えたら「🔽 のこり◯日を表示」。
- **✨AI総合分析**: 体調・睡眠・飲酒・飲水・食事・シフト・実績・予定を踏まえた総合分析（Gemini）。
  要因・アドバイスは**タグ（睡眠/運動/飲酒/食事/メンタル/生活リズム/仕事/その他）で分類表示**
- **💬AIに質問**（単体チャット）: `buildQAContext()` がアプリ内データ（過去約120日の1日ごとの
  体調メモ・睡眠・飲酒・食事・実際の行動・予定＋トレーニング・目標）をまとめ、「〇〇をやったのは
  いつ？」「〇〇を記録した日の体調は？」等のデータ照会に事実ベースで回答（総合分析の実行不要で使える）

---

### 4-8. 色のカスタマイズ
全ピッカーは共通の**17色パレット**（`EV_COLORS`／`colorPickerRow`）を使用：色相順に並び、
青系を分離、グレー・ブラック・ホワイトを含む。**既定色（色未設定の予定に使う色）も変更可**（`_defaultcolor`／`EV_DEFAULT`）。**各色に意味（例：食事・運動）を付けられ**（設定→「🎨 色の意味（凡例）」、
`_colorlabels`）。名前が表示されるのは意味変更画面のみ（他のピッカーはツールチップのみ）。以下で色を変更できる：予定・パターン、**定期ルーティン**（編集モーダル）、
**ルーティン項目/まとめグループ**（設定→ルーティン設定）、**勤務**（設定→シフト。保存値を
`SHIFT_TYPES` に反映＝全参照へ自動適用、`_shiftcolors`）、**射精**（記録モーダル・`_ejaccolor`）、**睡眠**（睡眠タブ・`_sleepcolor`）、**飲酒**（アルコールタブ・`_alccolor`）
の実績タイムライン色。すべてバックアップに含まれる。

## 5. データ保存（localStorage）

すべて端末内 localStorage に保存。主キー `health_v2`（logs＋dailies）に加え、機能別に
`health_v2_○○` の補助キーを使用。

**主な補助キー**:
`_todo`（デイリー実績）, `_todoitems`（ルーティン項目定義）, `_todo_order`（並び順）,
`_customtodos`（定期ルーティン）, `_schedule` / `_repeat`（予定）, `_planpatterns`（パターン予定）,
`_tasks`（TODO/買物）, `_goaltree`（目標）, `_routines`（実績まとめグループ）, `_shift`（シフト）,
`_rewards` / `_investactions` / `_investspends`（達成・未来投資）, `_ifthen`,
`_bacprofile` / `_alclimit` / `_cooloff` / `_nextdrink` / `_nightritual` / `_lifevision`（減酒系）,
`_connectgoal` / `_ctjournal` / `_decisbalance` / `_values` / `_changeplan`（つながり・MI）,
`_sleepgoal`（就寝リズム）, `_bodycomp`（体組成）, `_selfmodelog`（セルフモード履歴）, `_selfchecks`（セルフモードのチェック項目）,
`_lifemodes`（ライフモード定義）, `_modelog`（ライフモードのタイムライン）, `_defaultmode`（既定モード）, `_automode`（自動切替ON/OFF＋直近の自動判定）,
`_wakegoal`（目標起床時刻・シフト別）, `_nudgecfg`（ナッジON/OFF・端末通知・気分チェック時刻・基準日フォールバック時刻fallbackH・**項目別の設定rules・静かな時間帯quiet・再通知間隔repeat**。`nudgeCfgPayload()`/`applyNudgeCfg()`で読み書きを1本化し、**バックアップJSONにも`notifyCfg`として含める**）, `_nudges`（当日のナッジ状態）, `_todosec`（TODOの「やること／やりたいこと」の開閉）, `_widgetsections` / `_widgetorder`（ウィジェット）,
`_fontscale`（文字サイズ）, `_lastbackup`（最終バックアップ日時）, `_weather` / `_city`（天気キャッシュ）ほか。

---

## 6. データ安全性・バックアップ

- localStorage は端末・ブラウザ内のみの保存で、キャッシュ削除・機種変更で消える可能性がある
- **バックアップ**: 設定→データ管理から全データを JSON で出力／読込／閲覧（上書きせず確認）
- **バックアップ促進**:
  - ウィジェット先頭に常設バナー（14日以上未実施／未実施時に表示、ワンタップ出力・「後で」で当セッション非表示）
  - 設定に「最終バックアップ: N日前」を常時表示
  - 起動時トースト通知
- エクスポート対象は主要ユーザーデータを網羅（ウィジェット配置・クールオフ等の一時設定は対象外）

---

## 7. AI連携とセキュリティモデル

> **APIの正確な契約（リクエスト/レスポンス/エラーコード/制限）は [API.md](./API.md) を参照。**

### 呼び出し経路
アプリ（クライアント）→ `https://nuts024.com/health/api.php`（プロキシ）→ Google Gemini

- `callAIText(system, prompt)` … テキスト分析
- `callAIVision(system, prompt, imageBase64, mimeType)` … 画像つき（写真取り込み）
- クライアント側の設定は `AI_ENDPOINT` / `AI_SECRET` / `aiHeaders()` に一元化（キー変更は1箇所）

### セキュリティモデル（重要）
- クライアントは公開配信のため、`X-Secret-Key` は**露出前提**（隠せない）
- **実際の防御はサーバー側 `api.php` で行う**:
  - **Origin/Referer 厳格チェック**: `https://yuradream3838.github.io` 以外からのリクエストを拒否（他サイト・直叩き対策）
  - **レート制限**: IP単位（1時間15回／1日30回）＋全体1日上限（50回）
  - シークレットキーは補助的な二次チェック
- `api.php` は ConoHa 上にのみ設置し、Gemini APIキーを保持。**リポジトリには含めない**
- 現行 `api.php` は v4.1（セキュリティ強化＋画像対応）

---

## 8. PWA / デプロイ

- **配信**: GitHub Pages（main ブランチ）。`index.html` が `health.html` へ転送
- **Service Worker（sw.js, v2.1）**: HTML(ドキュメント)は常にネットワークから最新取得し、
  オフライン時のみキャッシュを使用（「古い版が出る」問題を回避）。
  キャッシュ対象は health.html / manifest.json / icon.svg。
  **`notificationclick`ハンドラ**で通知タップ時にアプリを前面化（既存タブをfocus、無ければ`./health.html#widget`を開く）。
- **更新反映**: `APP_VERSION` を上げてコミット→Pages 反映後、次回アクセスで最新取得
- **api.php の更新**: ConoHa 上で手動アップロード（リポジトリ管理外）

---

## 9. テスト

`test/` 配下に回帰テストを常備。

| ファイル | 依存 | 内容 |
|---|---|---|
| `test/syntax-check.js` | なし | 最大の `<script>` を抽出し `node --check` |
| `test/smoke.js` | Chromium＋playwright-core | 全タブ・全サブタブを実描画し JSエラー0を検証＋主要機能の存在確認 |

実行: `node test/syntax-check.js` / `node test/smoke.js` / `npm test`

---

## 10. 開発上の注意

- 変更はすべて `health.html` 1ファイルに対して行う
- リリース時は `APP_VERSION` を1つ上げる
- `api.php` は絶対にリポジトリへコミットしない（Gemini APIキーを含むため）
- 大きな単一ファイルのため、変更後は必ず構文チェック＋スモークテストを実行
